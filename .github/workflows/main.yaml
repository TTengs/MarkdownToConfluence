on: [push] #change

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Converting Markdown to Confluence
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v1
      - name: Conversion step
        uses: ./ # Uses an action in the root directory
        id: hello
        with:
#          build-args: DOC_PATH=./dinMor
          docs_path: './dinMor'
      # Use the output from the `hello` step
      - name: Get the status
        run: echo "Status is - ${{ steps.hello.outputs.status }}"
#      - name: Get the time
#        run: echo "The time is - ${{ steps.hello.outputs.time }}"
      - name: Hjælp mig
        run: | 
          whoami
          ls