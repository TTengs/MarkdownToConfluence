on: [push]

jobs:
  main_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Hello world action step
        id: hello
        uses: ./ #finds the action.yaml in the root
        with:
          atlassianToken: 'Testings'